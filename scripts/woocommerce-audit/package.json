{
  "name": "woocommerce-audit",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Adaptador y script de auditoría de SOLO LECTURA para la API REST de WooCommerce. Independiente de la app principal (src/) — no se importa desde ahí todavía.",
  "scripts": {
    "audit": "tsx src/auditCatalog.ts",
    "test": "node --import tsx --test src/**/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "dotenv": "^16.4.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
